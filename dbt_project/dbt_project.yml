name: azmart
version: "1.0.0"
config-version: 2
profile: azmart

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
clean-targets: ["target"]

models:
  azmart:
    staging:
      +materialized: view
      +schema: silver
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
